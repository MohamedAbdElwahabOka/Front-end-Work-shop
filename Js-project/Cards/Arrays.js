// Arrays - ordered lists of items

let featuredPosts = [
  "check out my netflix clone",
  "Here's the code for my project",
  "I've just relaunched my porfolio",
];

let Experience = ["CEO at scrimpa", "Frontend at google", "ITI Specialist"];

let socialMedia = ["twitter", "facebook", "instagram"];

// array push
// add an item to the end of an array
//array functions
//array.pop() - removes the last item in an array
//array.shift() - removes the first item in an array
//array.unshift() - adds an item to the beginning of an array

//array.splice() - removes an item by index position
//array.splice(1, 1) - removes the second item in an array
//array.splice(1, 0, "new item") - adds a new item at index position 1

//array.slice() - copies part of an array
//array.slice(1, 3) - copies the second and third items in an array

//array.indexOf() - finds the index of an item in the array
//array.indexOf("facebook") - returns 1

//array.join() - converts an array into a string
//array.join(", ") - converts an array into a string, separated by commas

//array.concat() - combines two or more arrays
//array.concat(socialMedia) - combines the featuredPosts and socialMedia arrays

//array.includes() - checks if an item exists in the array, and returns true or false
//array.includes("facebook") - returns true

//array.reverse() - reverses the order of items in an array

//array.sort() - sorts the items in an array
//array.sort() - sorts the items in an array in alphabetical order
//array.sort() - sorts the items in an array in numerical order

//array.forEach() - loops through an array
//array.forEach(function(item, index, array) {
//  console.log(item, index);
//});




